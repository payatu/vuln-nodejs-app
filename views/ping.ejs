<!DOCTYPE html>
<html>
<title>Command Injection lab</title>
<body>
<h3> Command Injection:</h3> <p> user supplied input directly passed to exec function without santization</p>
<h5> Vulnerable Code:</h5>
<h3>
<pre>
  const exec = require('child_process').exec
  ....  
  const ping = req.body.ping;
  exec('ping -c 3 ' + req.body.ping, function (err, stdout, stderr){
    output = stdout + stderr;
    console.log(output)
    res.render('ping', {
      output: output
    });
  })
</pre>
<form action="/ping" method="POST">
  <input type="text" name="ping">
  <input type="submit" value="ping">
</form>
<% if (output) { %>
<p> Command Output </p>
<pre>
<%= output %>
</pre>
<% } %>
</h3>
<br>
<h3>Fixed version</h3>
<p> In this version application uses `execFile` function from the `child_process` module that starts a specific program
and takes an array of arguments.
<h5> Fixed Code</h5>
<h3>
<pre>
  const execFile = require('child_process').execFile;
  ...
  execFile('/usr/bin/ping', ['-c', '3', ping1], function (err, stdout, stderr){
    pingoutput = stdout + stderr;
    res.render('ping', {
      pingoutput: pingoutput,
      output: null
    });
  });
</pre>
<form action="/ping" method="POST">
  <input type="text" name="ping1">
  <input type="submit" value="ping1">
</form>
<h5>
<% if (pingoutput) { %>
<p> Command Output </p>
<pre>
<%= pingoutput %>
</pre>
<% } %>
</h3>
</body>
</html>

